# Rust build artifacts
/target
**/target/
**/*.rs.bk

# Release packaging output (DMGs, checksums) from scripts/release-macos.sh
/dist/

# Coverage / profiling output
*.profraw
*.profdata
/coverage
*.tracy

# Per-run performance logs (keypress->photon traces, A/B benchmark dumps)
perf-*.jsonl
/.taskmaster/reports
/bench-results

# OS cruft
.DS_Store
Thumbs.db

# NOTE: Cargo.lock IS committed — this workspace produces a binary, so we want
# reproducible builds.
